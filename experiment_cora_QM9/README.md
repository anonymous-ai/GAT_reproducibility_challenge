# To run the Cora experiment, try:
python run.py --network=${network_type}

# To run the QM9 experiment, try:
python run_qm9.py --network=${network_type}

Where network_type is GAT, GAT_dot, GAT_const or GCN
